{
    "version": 2,
    "buildCommand": "pip install -r requirements.txt && mkdocs build",
    "outputDirectory": "site",
    "builds": [
        {
            "src": "docs/**/*",
            "use": "@vercel/static"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/site/$1"
        }
    ]
}
